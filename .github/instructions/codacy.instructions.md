---
description: Placeholder (Codacy integration instructions intentionally minimized per project decision)
applyTo: '**'
---

# Codacy Integration (Disabled)

The previous automated Codacy analysis requirements have been intentionally removed.

Current policy:
* No mandatory Codacy CLI runs after edits.
* Security / lint checks rely on existing project tooling (phpstan, phpcs, phpunit).

If Codacy is re-enabled later, restore the original rule set from version control history.
