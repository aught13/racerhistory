<?xml version="1.0"?>
<ruleset name="App">
    <rule ref="CakePHP"/>
    <rule ref="SlevomatCodingStandard.TypeHints.ReturnTypeHint.MissingNativeTypeHint">
        <exclude-pattern>*/src/Controller/*</exclude-pattern>
    </rule>

    <!-- Workaround: disable specific Slevomat sniffs that trigger runtime errors in this
         environment. These are temporarily disabled until an upstream fix is available. -->
    <rule ref="SlevomatCodingStandard.Functions.RequireTrailingCommaInCall">
        <severity>0</severity>
    </rule>
    <rule ref="SlevomatCodingStandard.Namespaces.ReferenceUsedNamesOnly">
        <severity>0</severity>
    </rule>



    <file>src/</file>
    <file>tests/</file>
</ruleset>
